# Template of environment file. This is a convenient way
# of passing multiple environment variables to an application instead
# of specifying them by command line
#
# Usage with docker-compose:
#    env_file: _dockerfiles/qxapp/python/.env
#
# will create these env variable at runtime
#
# NOTES:
#  - All variable related ports, host-ips, etc are better defined within Dockerfile SIMCORE_WEB_HOSTNAME
#    they can be overriden when running the container (i.e. via command line or docker-compose.yml)
#  - This file is more convenient for application variables, e.g. passwords, etc for that reason 
#    only a template version .env is under version control.
# 


# Web server. Defined in Dockerfile
# SIMCORE_WEB_HOSTNAME='0.0.0.0'
# SIMCORE_WEB_PORT=8080
# SIMCORE_WEB_OUTDIR=path/to/qx/source-output

# defines configuration setup (see CONFIG dict in config.py)
export SIMCORE_WEB_CONFIG='production'

# S4L computational service (thrift)
export CS_S4L_HOSTNAME='172.16.9.89'
export CS_S4L_PORT_APP=9095
export CS_S4L_PORT_MOD=9096


