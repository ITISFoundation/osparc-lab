PROJECT( oSPARClab )

CMAKE_MINIMUM_REQUIRED( VERSION 3.0 )

ENABLE_TESTING()

SET( CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/_bin )

ADD_SUBDIRECTORY( tools/ci-checks ci-checks )
ADD_SUBDIRECTORY( demos )

