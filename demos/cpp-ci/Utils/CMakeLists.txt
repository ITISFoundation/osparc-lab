SET( MY_TARGET_NAME Utils )

SET( SOURCE_FILES 
    PrintHelloWorld.cpp
    Test.cpp
)

#SET( LINK_LIBRARIES
#    
#)

ADD_LIBRARY( ${MY_TARGET_NAME} ${SOURCE_FILES} )
#TARGET_LINK_LIBRARIES( ${MY_TARGET_NAME} ${LINK_LIBRARIES} )
